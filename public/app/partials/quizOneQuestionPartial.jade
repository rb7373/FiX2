.raw
	h4 {{currentPage}}) {{currentQuestion.question}}
.raw
	br
	form
		div(ng-repeat='choice in currentQuestion.choices')
			input(name ='{{currentQuestion._id}}' value='{{$index}}', type='radio', ng-model ='currentQuestion.selectedAnswer')
			label {{choice}}
			br



		

